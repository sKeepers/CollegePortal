# Текущая работа

## Назначение

Файл фиксирует состояние рабочей сессии CollegePortal. Это точка входа для нового чата, агента или инженера после прочтения `AGENTS.md`.

Обновлять перед окончанием сессии, переходом к существенно другой задаче и после каждого развёртывания на DEV.

## Обновлено

- Дата: 2026-08-08
- Рабочая копия: `C:\!Projects\CollegePortal`
- DEV checkout: `/home/andale/CollegePortal`, worktree задачи `/home/andale/CollegePortal/.worktrees/person-documents-completeness`

## Документы на уровне человека и полнота карточки студента (08.08.2026)

Задание — [STUDENT_CARD_COMPLETENESS_PLAN.md](STUDENT_CARD_COMPLETENESS_PLAN.md). Ветка `feature/person-documents-completeness`. Выполнено целиком.

**Структура.** Миграция `2026_08_09_000001_lift_admission_documents_to_person`: `person_id` добавлен в `admission_education_documents` и `admission_document_files` и заполнен из `applicants.person_id`; `applicant_id` стал необязательным во всех трёх таблицах документов и остался историческим признаком происхождения; `person_id` у документа об образовании стал обязательным. Откат удаляет только документы без абитуриента — данные приёмной комиссии он не трогает.

**Расчёт полноты — один.** `AdmissionDocumentReadinessService` получил разбор по человеку: `forStudent`, `forStudents` (пакетно, для реестра), `studentCardSummary`, `incompleteStudentIds` и жёсткий `assertStudentCardComplete`. Выбор действующего документа человека вынесен в `PersonDocumentService`, к нему же обращается приёмная комиссия. Второго расчёта не заведено.

**Что делает система теперь.**

- Студент без заявления в приёмной комиссии может иметь паспорт и документ об образовании: `POST api/students/{id}/identity-documents` и `.../education-documents`, `applicant_id` остаётся пустым.
- Человек заводится для каждого студента, иначе документ некуда прикрепить. При пустом СНИЛС система ищет совпадения по ФИО и дате рождения и возвращает их в `warnings.duplicate_candidates` — предупреждение, а не автосвязывание.
- Реквизиты паспорта с формы студента сохраняются как документ человека; новая версия документа создаётся, только если реквизиты действительно изменились.
- Реестр показывает признак «Полная / Неполная» и умеет фильтровать `completeness=incomplete`. Карточка студента показывает, чего не хватает, и позволяет добавить документ об образовании.
- Жёсткие блокировки: `mark-exported` в ФИС ГИА и ФРДО — только для проверенного пакета; неполная карточка делает запись пакета невалидной; диплом и приложение к диплому не выдаются; номер приказа о зачислении нельзя записать или изменить.
- Импорт студентов принимает паспорт и документ об образовании, но не требует их: строка без них не ошибка. Обе точки загрузки — «Универсальный импорт» и кнопка в реестре — заводят человека и кладут документы ему, поэтому один и тот же файл даёт одинаковый результат. Выгрузка отдаёт те же колонки.
- Роль `study_records` получает напоминание о неполных карточках раз в восемь часов через колокольчик, ссылка ведёт в реестр с фильтром. Доступ роли к новым маршрутам покрыт тестом.

**Попутно исправлено.** Поиск вероятных дублей по ФИО и дате рождения не работал вовсе: `people.birth_date` хранится как `2008-09-01 00:00:00`, а сравнение шло со строкой `2008-09-01`. В `PersonService` и `PersonDuplicateService` сравнение переведено на `whereDate`.

**Проверки.** `php artisan test` — `417 passed (2670 assertions)` на SQLite и `416 passed` на PostgreSQL 17 (последний тест добавлен после прогона на PostgreSQL). Миграция прогнана на PostgreSQL 17 вперёд → назад → вперёд, внешние ключи и признаки `NOT NULL` проверены запросом к `information_schema`. Сборка фронтенда успешна. `assert-path-policy.sh` — OK.

**Не сделано намеренно.** Импорт не блокирует строку с номером приказа и без документов: по решению владельца строка без документов не считается ошибкой, а импорт — путь загрузки исторических данных. Блокировка приказа стоит на API карточки студента.

## Состояние на 08.08.2026 (вечер)

- **PROD `192.168.34.17` работает на `0.8.0-rc7`**, коммит сборки `900e3dc6c`, тег `v0.8.0-rc7`. Ствол `develop` на том же коммите и опубликован в origin.
- При обновлении применилась миграция `2026_08_08_000001_create_buildings_and_access_points`. Проверка снаружи: `version.json` — `0.8.0-rc7` и build `900e3dc6c`, `health/live` и `health/ready` — `200`, `check.sh` внутри обновления прошёл целиком.
- Резервные копии PROD за день: `20260808-070925` (перед rc6), `20260808-101001` (до всех работ), `20260808-112811` (перед очисткой данных), `20260808-114731` (перед rc7).
- **Рабочие данные PROD очищены.** Осталось: 1 администратор, 11 ролей, 144 права, 29 настроек, 179 позиций справочников. Таблицы данных пусты. Журнал аудита очищен по решению владельца. Скрипт — `/home/andale/reset-working-data.sh`.
- Проверки перед релизом: `php artisan test` — `404 passed (2593 assertions)`; сборка фронтенда успешно; `assert-path-policy.sh` — OK. Ранее на этой же ветке прогон на PostgreSQL 17 — `400 passed`, миграция корпусов проверена в обе стороны.
- Ветка `feature/access-buildings-muster` влита в `develop` fast-forward и опубликована.

## Блокеры и риски

- Всё опубликовано в origin: `develop` на `88c989bf9`, тег `v0.8.0-rc6`, ветка `feature/access-buildings-muster`. CI зелёный и на `develop`, и на ветке. Работы в единственном экземпляре не осталось.
- Ветка не влита в `develop`: решение о вливании и о сборке `0.8.0-rc7` за владельцем.
- Пароль учётной записи генерируется как пятизначное число (`random_int(10000, 99999)`) — и при выдаче, и при сбросе. Для контура с реальными ПДн это слабо; выбор политики пароля за владельцем, менять в одиночку не стал.
- `ARCH-001`: в `EnsurePermission` права выводятся из префикса URL, при отсутствии совпадения молча возвращается `reference.manage`. Новые маршруты `api/access/muster`, `api/access/buildings` и `api/access/points` в таблицу внесены явно.
- `SEC-002`: bearer-токен в браузерном хранилище. Стоп-фактор перед загрузкой реальных персональных данных, блокирует `AUTH-005` и вход через мессенджеры.
- На PROD нет контингента: импорт предстоит.

## Изменены ли DEV и PROD

- **PROD изменён:** обновлён до `0.8.0-rc6`. Документы уровня человека на PROD не разворачивались.
- **DEV изменён:** `develop` доведён до коммита релиза, создана ветка `feature/access-buildings-muster` с четырьмя коммитами. Позже создана и влита ветка `feature/person-documents-completeness`; в рабочих базах DEV миграция документов не применялась, проверки шли в контейнере на отдельных тестовых базах, они удалены.
- **Аудит 08.08.2026 (вечер) ничего не изменил.** Работал во временном worktree `.worktrees/audit-portal` от `origin/develop` (`09c74c975`); worktree, ветка `audit/portal-2026-08-08` и тестовая база `college_portal_audit` удалены после прогонов. Проверочные тесты не коммитились, их код приложен к отчёту. PROD не трогался. Локально не закоммичен только сам отчёт `docs/AUDIT_2026-08-08.md`.

## Решения владельца, принятые 08.08.2026

- **SEC-002 закрывается переносом токена в httpOnly Secure SameSite cookie с CSRF-защитой.** Полумеры вроде sessionStorage отклонены. Это следующая крупная задача: затрагивает весь фронтенд и мобильный контур, разблокирует `AUTH-005` и вход через Telegram и MAX.
- **Справки и зачётные книжки** не начинать, пока не получены реальные бланки, которые в ходу у «Учебной части 2». После этого — план и оценка, затем разработка.
- **Объединение дублей с приставкой UAT** выполняется вместе со следующим обновлением PROD: там уже включается режим обслуживания и рвутся сессии, отдельного окна не нужно.

## Очистка PROD перед вводом реальных данных

- Скрипт лежит на PROD: `/home/cp-agent/reset-working-data.sh`. Запуск — `sudo bash /home/cp-agent/reset-working-data.sh`. Требует ввести `DELETE`, сам снимает резервную копию перед очисткой и печатает счётчики до и после.
- Удаляет всё, кроме миграций, ролей, прав, настроек, справочников и учётных записей администраторов. Журнал аудита очищается — решение владельца. Учётные записи не-администраторов удаляются.
- Проверено: внешних ключей из защищённых таблиц в таблицы данных нет, поэтому `TRUNCATE ... CASCADE` не заденет администраторов. Если администратор не найден, транзакция откатывается целиком.
- Резервная копия, снятая до всех работ: `/var/backups/college-portal/20260808-101001`.

**Важно про порядок.** Экспорт сотрудников появился только в ветке `feature/access-buildings-muster`, на PROD его ещё нет. Если список сотрудников нужен файлом — сначала `0.8.0-rc7` на PROD, потом выгрузка, и только потом очистка. Иначе сотрудники останутся только внутри дампа резервной копии.

## Решения владельца по данным студентов от 08.08.2026

Подробное задание — [STUDENT_CARD_COMPLETENESS_PLAN.md](STUDENT_CARD_COMPLETENESS_PLAN.md). Коротко:

- Паспорт, документ об образовании и СНИЛС обязательны **для полноты карточки, а не для сохранения**. Жёсткие блокировки остаются на ФИС ГИА, ФРДО, приказ о зачислении и диплом.
- СНИЛС перестаёт быть обязательным при сохранении: предупреждение на форме, сохранять разрешено. Формат проверяется, если значение введено.
- Импорт студентов принимает паспорт и документ об образовании, но не требует их.
- Роль `study_records` периодически получает напоминание о недостающих данных.
- Импорт журнала успеваемости решено **не делать**: он обходит подписание журнала и одобрение переоткрытия.
- Отдельная задача: экспорт расписания. Импорт расписания есть, экспорта нет.

Препятствие, которое надо снять первым: паспорт и документ об образовании привязаны к абитуриенту (`applicant_id` обязателен), а у документа об образовании нет `person_id` вовсе. Студент, не проходивший приёмную комиссию, не может иметь этих документов. Документы нужно поднять на уровень `Person`. **Снято 08.08.2026** — см. раздел выше.

## Следующие действия

1. **Разобрать отчёт аудита** [AUDIT_2026-08-08.md](AUDIT_2026-08-08.md): 13 дефектов, ничего не исправлялось, порядок работ за владельцем.
2. **Экспорт расписания.** Импорт есть, экспорта нет. Колонки брать у `ScheduleImportHandler`, как сделано для сотрудников и студентов.
3. **Полнота карточек педагогов и сотрудников.** Шаблон преподавателя самый бедный: ни даты рождения, ни СНИЛС, ни адреса. У сотрудников в шаблоне нет даты рождения и СНИЛС, хотя обработчик их принимает. Расчёт полноты уже есть — расширять `AdmissionDocumentReadinessService`, а не заводить второй.
4. **Собрать `0.8.0-rc8`,** когда накопится. Ствол уже впереди PROD на исправление экспорта студентов и на документы уровня человека. **Миграция `2026_08_09_000001` меняет обязательность колонок — перед обновлением PROD снять резервную копию и учесть, что её откат удаляет документы, заведённые вне приёмной комиссии.**

## Ждут владельца

- **ФИС ГИА:** адрес тестового контура, сертификат и учётные данные. Без них `SoapFisTransport` остаётся заглушкой.
- **SMTP:** пароль ящика `portal@skki.ru` на `smtp.mail.ru`. Через Beget не пройдёт SPF.
- **`SEC-002`:** подход выбран (httpOnly cookie с CSRF), не назначен срок. Блокирует `AUTH-005` и вход через Telegram и MAX.
- **Аудит портала:** выполнен 08.08.2026, отчёт — [AUDIT_2026-08-08.md](AUDIT_2026-08-08.md). Промпт: [prompts/0_PORTAL_AUDIT.md](prompts/0_PORTAL_AUDIT.md). Найдено 13 дефектов; порядок исправления — в разделе 4 отчёта. Исправлены и влиты в `develop`: **находка 1** (ключ ограничителя входа), **находки 3 и 4** (BOM в импортах и выгрузках CSV). Следующая по очереди — **находка 2**: доступ ролей к экранам, 12 пар «роль — экран», роль `study_records` нерабочая; вместе с ней внести в CI тест «роль × экран».

### Что сделано по находкам 3 и 4

- Формирование CSV сведено в `App\Support\Csv\CsvExport`, чтение самодельных импортов — в `App\Support\Csv\CsvImport`. Своих `fopen('php://output')` в приложении не осталось.
- Выгрузки журнала и UAT перешли с запятой на точку с запятой — были единственными, кто отличался.
- Импорты справочников (`*CsvService`) на `CsvImport` **не переводились**: они BOM снимают сами и работают верно. Их место — в `UniversalImportService`, где для учебных планов, нагрузки и расписания обработчики уже написаны и покрыты тестами. Это отдельная задача: сейчас в портале два независимых импорта одних и тех же сущностей.
- Шов закреплён тестом `CsvSeamTest`: маркер во всех четырнадцати выгрузках, отсутствие дубликатов после Excel у нагрузки, учебных планов и экзаменов, приём файла с запятой. Проверено, что на старом коде тест падает.

Снято с повестки: объединение дублей с приставкой UAT — учётные записи удалены вместе с рабочими данными PROD 08.08.2026.

## Git-состояние (журнал 03.08.2026)

- Active worktree branch: `sync/sync-001-local`
- Last deployed DEV checkpoint: `22c4883de`
- DEV branch: `feature/uat-002-1-final-stabilization`
- GitHub branch: `origin/feature/uat-002-1-final-stabilization` развёрнута на DEV до `22c4883de`.
- Локальный HEAD: `22c4883` на ветке `sync/sync-001-local`; последний известный DEV HEAD: `22c4883de`.
- `SYNC-001` объединил GitHub `SEC-001` с DEV UAT-002.2 и был развёрнут на DEV.
- На DEV применена миграция `2026_07_30_010000_add_lookup_and_expiration_to_api_tokens`.
- Проверки после развёртывания: `php artisan test` — `347 passed (2215 assertions)`; `npm run build` завершилась успешно; health endpoint вернул `200`.
- На DEV развёрнуты карточка оценок и адаптация профилей устройств после успешной production-сборки.
- Legacy `ScheduleLesson` теперь открывает конкретный `JournalLesson` через `legacy_schedule_lesson_id`; создание идемпотентно, переносит тему занятия и формирует roster студентов.
- Проверки исправления: `JournalEngineApiTest` — `10 passed (64 assertions)`; `npm run build` завершилась успешно.
- Teacher journal: список студентов растягивает страницу, подсказки проходной показывают результат для каждого студента, недоступные административные быстрые действия скрыты, а `Мои занятия` показывает все занятия преподавателя.
- Последняя проверка: `JournalEngineApiTest` — `10 passed (67 assertions)`.
- Подписанный журнал показывает подтверждение перед подписью. Преподаватель может направить запрос редактирования, а пользователь с `journal.reopen` одобряет или отклоняет его; одобрение переоткрывает журнал и фиксируется в audit.
- Миграция DEV `2026_08_02_010000_create_journal_edit_requests_table` применена. `JournalEngineApiTest`: `11 passed (75 assertions)`.
- Pending запросы отображаются в журнале администратора и на admin dashboard. Обе точки открывают конкретный журнал для одобрения или отклонения, а после решения запрос исчезает из обоих списков. `JournalEngineApiTest`: `11 passed (81 assertions)`.
- Dashboard использует единое представление версии для widget и `О системе`; masonry grid пересчитывает spans через `ResizeObserver`, устраняя пустоты между карточками.
- `POST admin/demo-data/reset` очищает рабочие данные DEV через `TRUNCATE ... RESTART IDENTITY CASCADE`, сохраняя пользователей, роли, permissions, settings и справочники. Устаревшая кнопка `Очистить демо-данные` заменена на `Очистить рабочие данные DEV`.
- Reset выполнен на DEV: сохранены 12 пользователей, 11 ролей, 156 permissions и 26 settings; `Student`, `Teacher`, `ScheduleLesson`, `JournalLesson` и `JournalEditRequest` имеют по 0 записей.
- До изменений создана проверенная резервная копия PostgreSQL DEV: `/home/andale/CollegePortal/backups/pre-change-20260802-1745.sql`, 438786 bytes, SHA-256 `d306daa896499d89eb60fdcefcdc051fc750d3d9223ca32d58b2066811f14112`.
- Кадровый контур: подразделения и должности создаются без ручного кода, автоматически получают уникальный code и показывают ошибку сохранения. Сотрудник с `is_teacher` получает профиль `Teacher` для той же `Person`; дополнительные `EmployeeAssignment` поддерживают внутреннее и внешнее совместительство.
- Пропуск автоматически отзывается при удалении или деактивации `Student`, `Teacher` либо `User`; событие фиксируется в audit.
- Проверка `Заявления_принятые_20260802203415.xls`: 243 заявления, все СНИЛС проходят checksum. Импорт не выполнялся: текущий обработчик не использует `SnilsService` и не переносит паспорт в foundation documents; UI дополнительно блокирует apply при числе строк, отличном от 149.
- Полное архивирование PostgreSQL развёрнуто: защищенный API `/api/admin/database-backups`, `pg_dump`/`pg_restore` через безопасный массив аргументов, аварийный снимок перед restore, audit, UI в `DataManagementPage` и Pinia store. PostgreSQL client 17 добавлен в backend image.
- Проверки: `PostgresBackupServiceTest` и `DatabaseBackupApiTest` — `4 passed (17 assertions)`; `npm run build` завершилась успешно; health endpoint вернул `200`.
- Создан и проверен первый архив через сервис: `manual-20260802-192823-92922147-fd1d-49d4-9c7d-7449a91ddbbc.dump`, 397065 bytes. Восстановление действующей DEV-базы намеренно не запускалось.
- Исправлены права DEV runtime storage: `storage` и `storage/app/private/postgresql-backups` принадлежат `www-data`; создание второго архива от имени web-процесса подтверждено (`manual-20260802-195519-21088212-5f81-48ca-9172-a0f3f99a3eb1.dump`, 396791 bytes).
- XLSX-шаблон и поток `preview -> validation -> confirm` сотрудников развёрнуты в Universal Import; пустой табельный номер генерируется автоматически. `UniversalImportApiTest` и `HrFoundationApiTest`: `18 passed (117 assertions)`; frontend build завершился успешно.
- Новые студенты и foundation-абитуриенты требуют валидный СНИЛС; Student API/CSV/Universal Import создают или переиспользуют Person по `snils_hash`; ФИС apply блокируется при пустом или некорректном СНИЛС. DEV tests: `31 passed (145 assertions)`.
- В ветке переноса добавлены `**/bin/` и `**/obj/` в `.gitignore`. В root worktree остаются пользовательский `docs/UAT_002_REPORT.md` и локальный .NET build artifact `obj/`; не удалять без отдельного решения.
- DEV изменён; PROD не изменялся.

## Доступ к DEV

- Browser portal: `https://84.54.208.134:5443`
- Internal DEV portal: `https://192.168.34.114:5443`
- Health check: `http://127.0.0.1:8001/health/live`
- Containers: `docker compose -f /home/andale/CollegePortal/docker-compose.yml`
- Never put passwords, tokens, private keys, or personal data in this file.

## Текущая задача

Завершение UAT-002.2 и подготовка проекта к переносу на другой компьютер.

GitHub Issues доступны на DEV только для чтения через `gh`. Текущий обзор: [GitHub Issue Review 2026-08-01](GITHUB_ISSUE_REVIEW_2026-08-01.md); не изменять Issues без явной задачи.

Принятые требования:

1. Вход: показ пароля, сохранение браузером и выбор постоянной или сессионной авторизации.
2. Преподаватель: ограниченный собственными занятиями журнал с посещаемостью и оценками, без редактора расписания.
3. Студент: только личное расписание, режимы недели и месяца, детальные оценки без рабочего пространства журнала преподавателя.
4. Проходная: компактный сканер для телефона с низкой нагрузкой.
5. Адаптивная маршрутизация по ролям для телефона, планшета, HD, FullHD и широких desktop-экранов.

## Проверенные checkpoint

- `328572e`: динамический QR, защита от повторного использования, ограничение журнала студента.
- `c3c2a6d`: доступ студента к расписанию и мобильная навигация по датам.
- `88b1f83`: компактное рабочее пространство мобильного сканера.
- `86108ff`: личный QR-пропуск с обратным отсчётом.

## Следующие действия

1. Проверить на реальном телефоне то, что раньше было невозможно: камеру сканера на `https://portal.skki.ru/access/mobile-scanner` и личный пропуск. Это снимает вопрос по `MOB-003`.
2. Проверить импорт сотрудников на реальном файле: колонки «Рабочий график» и «Создать учетную запись» возвращены и покрыты тестами, но на живых данных не прогонялись.
3. Решить судьбу ветки `feature/access-control-foundation`. Документы из неё перенесены, код — нет; сливать нельзя, переделывать инкрементально. Решение за владельцем системы.

## Блокеры

- ФИС-выгрузка не должна применяться без ручной проверки dry-run и подтверждения сопоставления конкурсов.
- `SEC-002`: bearer-токен в браузерном хранилище. Стоп-фактор перед обработкой реальных персональных данных.
- Проверка Telegram-виджета, камеры телефона и установки PWA возможна только на `https://portal.skki.ru`, то есть после установки релиза на PROD.

## Чек-лист передачи

Before ending a session or opening a new chat:

1. Run `git status --short`, `git diff --check`, and `git log --oneline -10`.
2. Update this file with branch, local/DEV HEAD, uncommitted work, verified checks, blockers, and exact next actions.
3. Commit completed logical work; do not commit incomplete or unreviewed changes unless explicitly requested.
4. State whether DEV and PROD were changed.
5. Offer a new chat when the task is complete, when the context is near its practical limit, or when the next task is independent.

## Запрос для нового чата

```text
Read AGENTS.md, docs/ACTIVE_WORK.md, TASKS.md, and docs/UAT_002_REPORT.md.
Run git status --short, git diff --check, and git log --oneline -10.
Compare the local branch and DEV HEAD stated in ACTIVE_WORK.md.
Continue only the listed Next Actions. Do not discard uncommitted work.
```
