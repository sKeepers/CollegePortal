# Uninstall

Uninstall is destructive for the installed application directory and Docker volumes, but it does not delete backups.

```bash
sudo /opt/college-portal/installer/uninstall.sh
```

The script shows what will be removed, offers to create a backup, and requires the exact confirmation phrase `DELETE COLLEGEPORTAL`.
