# CollegePortal Gateway Diagnostics

Use `07-collect-diagnostics.cmd` to collect service state, version, health, route to `10.0.3.1` and basic local status. Review the output before sharing.

Diagnostics must redact shared secrets, credentials, HMAC headers, SOAP bodies and personal data. Real FIS data must not be attached to GitHub Issues.
