# FIS Support Request

## Closed Question

Support clarified that the FIS GIA and Admissions exchange is XML-over-HTTP, not SOAP/WSDL.

## Remaining Administrative Questions

Request the official materials or confirmation for:

- XSD version used by TEST;
- exact root/namespaces for `GetTestDictionariesList`;
- request and response examples for read-only dictionary methods;
- authentication placement and field names;
- required Content-Type and encoding;
- documented error response format.

Do not send real personal data in support requests.
