# Документы абитуриента Admissions Foundation

Документ фиксирует реализацию BACK-005: структурированные идентификационные сведения, документы об образовании, private files и расчет готовности заявления. Это foundation-слой новой приемной комиссии; legacy-раздел `/admissions` и старые `applicant-applications` не изменяются.

## Источники

Локально проверены официальные материалы ФИС ГИА и Приема 4.9:

| Материал | Локальный путь | SHA-256 |
| --- | --- | --- |
| XSD метода импорта | `docs/external-services/ФИС ГИА и Приема/XSD схема метода импорта Сервиса автоматизированного взаимодействия.xsd` | `7158AE7D523D3B08784A29ED0CDB4ACE025695E30526285EBABB3D93C093F840` |
| Спецификация сервиса | `docs/external-services/ФИС ГИА и Приема/Спецификация сервиса автоматизированного взаимодействия с информационными системами.pdf` | `0DF1E7381ADFCF129C00146EFBFBFD2E39B403675D72988519A931ABC0680C31` |

Файлы спецификации используются как локальный private registry контрактов. BACK-005 не коммитит raw XSD/WSDL/DISCO и не выполняет SOAP, Import, Validate, Delete или отправку в ФИС.

## Модель

### СНИЛС

СНИЛС хранится на уровне `Person`, потому что это общий идентификатор человека, а не отдельного заявления.

| Поле | Назначение |
| --- | --- |
| `people.snils` | нормализованный СНИЛС для внутренней работы |
| `people.snils_hash` | SHA-256 нормализованного СНИЛС для поиска дублей без раскрытия значения |

API-ресурсы по умолчанию возвращают `snils_masked` и `has_snils`. Полное значение доступно только пользователям с правом `admissions.document.download_sensitive` или административным правом на обновление Person.

### Документ личности

Таблица `admission_identity_documents` связывает документ с `Applicant` и `Person`.

Основные поля:

- `applicant_id`;
- `person_id`;
- `document_type_id`;
- `series`;
- `number`;
- `number_hash`;
- `issue_date`;
- `issued_by`;
- `division_code`;
- `birth_place`;
- `release_country_id`;
- `release_place`;
- `gender_id`;
- `nationality_type_id`;
- `fis_uid`;
- `verification_status`;
- `is_primary`;
- `archived_at`.

По XSD подготовлено соответствие с `TIdentityDocument`: `UID`, `OriginalReceivedDate`, `LastName`, `FirstName`, `MiddleName`, `GenderID`, `DocumentSeries`, `DocumentNumber`, `SubdivisionCode`, `DocumentDate`, `DocumentOrganization`, `IdentityDocumentTypeID`, `NationalityTypeID`, `BirthDate`, `BirthPlace`, `ReleaseCountryID`, `ReleasePlace`.

### Документ об образовании

Таблица `admission_education_documents` связывает документ с `Applicant`.

Основные поля:

- `applicant_id`;
- `document_type_id`;
- `education_level_id`;
- `series`;
- `number`;
- `number_hash`;
- `issue_date`;
- `document_organization`;
- `graduation_year`;
- `country_id`;
- `average_score`;
- `has_attachment`;
- `fis_uid`;
- `verification_status`;
- `is_primary`;
- `archived_at`.

По XSD подготовлены целевые структуры: `TSchoolCertificateDocument`, `TSchoolGeneralCertificateDocument`, `TMiddleEduDiplomaDocument`, `TBasicDiplomaDocument`, `TForeignStateEduDocument`, `TAcademicDiplomaDocument`, `TEduCustomDocument`.

### Файлы документов

Таблица `admission_document_files` хранит metadata private-файлов и связь с документом:

- `applicant_id`;
- `application_id` nullable;
- `identity_document_id` nullable;
- `education_document_id` nullable;
- `category`;
- `original_name`;
- `generated_name`;
- `storage_path`;
- `mime_type`;
- `extension`;
- `size_bytes`;
- `sha256`;
- `uploaded_by`;
- `uploaded_at`;
- `archived_at`.

Файлы сохраняются на `Storage::disk('local')`, то есть в private storage приложения. API-ресурсы не отдают `storage_path`. Удаление на этом этапе является архивированием metadata; физический файл не удаляется.

## Справочники

BACK-005 добавляет через Reference Data:

- `admission_identity_document_types`;
- `admission_education_document_types`;
- `admission_document_verification_statuses`;
- `admission_document_file_categories`;
- `education_levels`.

Статусы проверки:

- `received`;
- `pending_review`;
- `verified`;
- `rejected`;
- `replacement_required`;
- `archived`.

## API

| Endpoint | Метод | Назначение | Permission |
| --- | --- | --- | --- |
| `/api/admissions/applicants/{applicant}/snils` | `PATCH` | обновить СНИЛС Person через Applicant | `admissions.document.update` |
| `/api/admissions/applicants/{applicant}/identity-documents` | `GET` | список документов личности | `admissions.document.view` |
| `/api/admissions/applicants/{applicant}/identity-documents` | `POST` | создать документ личности | `admissions.document.create` |
| `/api/admissions/identity-documents/{document}` | `GET` | карточка документа личности | `admissions.document.view` |
| `/api/admissions/identity-documents/{document}` | `PATCH` | обновить документ личности | `admissions.document.update` |
| `/api/admissions/identity-documents/{document}` | `DELETE` | архивировать документ личности | `admissions.document.delete` |
| `/api/admissions/applicants/{applicant}/education-documents` | `GET` | список документов об образовании | `admissions.document.view` |
| `/api/admissions/applicants/{applicant}/education-documents` | `POST` | создать документ об образовании | `admissions.document.create` |
| `/api/admissions/education-documents/{document}` | `GET` | карточка документа об образовании | `admissions.document.view` |
| `/api/admissions/education-documents/{document}` | `PATCH` | обновить документ об образовании | `admissions.document.update` |
| `/api/admissions/education-documents/{document}` | `DELETE` | архивировать документ об образовании | `admissions.document.delete` |
| `/api/admissions/identity-documents/{document}/files` | `POST` | загрузить private-файл к документу личности | `admissions.document.update` |
| `/api/admissions/education-documents/{document}/files` | `POST` | загрузить private-файл к документу об образовании | `admissions.document.update` |
| `/api/admissions/document-files/{file}/download` | `GET` | скачать private-файл | `admissions.document.download_sensitive` |
| `/api/admissions/document-files/{file}` | `DELETE` | архивировать файл | `admissions.document.delete` |
| `/api/admissions/applications/{application}/document-readiness` | `GET` | расчет готовности документов заявления | `admissions.document.view` |

## Готовность заявления

Расчет разделяет три уровня:

| Уровень | Условие |
| --- | --- |
| `internal_complete` | есть основной документ личности, СНИЛС, основной документ об образовании, файл личности и файл образования |
| `review_complete` | `internal_complete=true` и оба документа имеют статус `verified` |
| `fis_data_ready` | пока всегда `false`, потому что XML-маппинг и ФИС-пакет не входят в BACK-005 |

Регистрация заявления остается обратно совместимой. Жесткая проверка документов включается только при `confirm_required_fields=true`.

## Безопасность

- Скан-копии хранятся только в private storage.
- `storage_path` не возвращается в API.
- Полные серия, номер и СНИЛС маскируются в ресурсах без sensitive permission.
- В audit не пишутся `snils`, `snils_hash`, `series`, `number`, `number_hash`, `storage_path`, `stored_path`, `fis_raw_data`.
- Повторная загрузка того же файла к тому же документу отклоняется по `sha256`.
- Разрешены только `PDF`, `JPG`, `JPEG`, `PNG` до 15 МБ.

## Ограничения BACK-005

- Не реализуется frontend.
- Не реализуется генерация XML.
- Не выполняется XSD validation пакета заявления.
- Не выполняется SOAP, Gateway, Import, Validate, Delete или отправка в ФИС.
- Legacy `/admissions` не мигрируется и не смешивается с Admissions Foundation.

## Следующие шаги

- BACK-006: подключить документы к frontend workspace.
- BACK-007: реализовать индивидуальные достижения и экзамены foundation.
- FIS Admissions mapping: отдельная задача после утверждения полного XML-пакета и правил выгрузки.
