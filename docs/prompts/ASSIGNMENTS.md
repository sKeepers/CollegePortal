# Назначения по чатам

**Обновлено: 11.08.2026, сверено со стволом `edb4c52`.**

Ведёт координирующий чат. Рабочие чаты **читают, но не правят**. Доска «Кто чем занят прямо сейчас» в [ACTIVE_WORK.md](../ACTIVE_WORK.md) — наоборот, её ведёте вы.

**Раздел здесь расходится с доской — верен этот файл.**

## Как пользоваться

1. `git fetch dev && git merge --ff-only dev/develop`
2. Найди свой раздел **по названию, не по номеру**.
3. Делай «Сейчас», закончив — бери «Дальше», не спрашивая.
4. Заявляй задачу на доске **до** того, как заводишь ветку.
5. **Не оставляй работу незакоммиченной.** Один чат остановился, не сохранив пять готовых файлов; их нашли в worktree чудом. Коммить в свою ветку хотя бы черновиком, даже если не доделал.

## Как владелец называет чаты

| Владелец говорит | Раздел здесь |
| --- | --- |
| Первый | «Учётные записи, роли и демо-данные» |
| Второй | «Единая карточка человека» |
| Третий | «ФИС ГИА и приёмная комиссия» |
| Четвёртый | «Мобильные кабинеты» |

---

## Чат «Мобильные кабинеты»

**Сейчас: дописать в обход прав два раздела и перезапустить его на текущем стволе. Это единственное, что держит релиз.**

Обход пройден и дал ноль расхождений — но **на стволе `ffdb8fa`**, а после него влились ваши `MOB-002` (`6c04912`) и `MOB-004` (`d1e14a6`). В таблице `SECTIONS` файла [scripts/deploy/check-role-access.sh](../../scripts/deploy/check-role-access.sh) есть только `mobile/student` и `mobile/teacher`: **кабинеты куратора и администратора живым обходом не проверялись ни разу.**

Дописать надо две строки в том же формате «название\|право\|пути»:

```
Мобильный кабинет куратора|mobile.curator.view|mobile/curator
Мобильный кабинет администратора|mobile.admin.view|mobile/admin
```

Три вещи, чтобы не потратить на это день:

- **Учётные записи заводить не нужно.** `curator@local` завела `ffdb8fa` в `PortalUserSeeder`, `admin@local` был всегда.
- **Маршруты с `{group}` в обход не ставьте** — там нужен идентификатор группы, а обход ходит по путям без параметров. Разграничение по куратору проверяет `MobileCuratorAccessTest`, и он перебирает маршруты списком.
- **Таблицу разделов не перестраивайте, только допишите.** Скрипт по происхождению за областью демо-данных; она сейчас в работе над демо-набором, и прерывать её ради двух строк дороже, чем отдать вам ваши же кабинеты.

**Ноль расхождений — так и напишите, этого достаточно, чтобы я собрал `rc8`.** Найдёте расхождение — не чините, отдайте списком: роль, раздел, ожидалось, получено.

**Дальше: `MOB-003`** — мобильное рабочее место проходной: компактный режим, устойчивость к потере фокуса и блокировке экрана, ручной ввод как запасной путь.

На доске вы записали, что она упирается в установку релиза на PROD. Принимаю как есть и уточняю задачу: **делайте всё, что делается без боевого домена, а отдельным списком перечислите, что останется проверить после установки** — я включу этот список в план выкатки. Так задача не будет стоять целиком из-за последнего шага.

Помните собственную находку: **подавление дублей — по значению кода, а не по времени.** CP2-код одноразовый, любое временное окно однажды истечёт, и следующий же кадр запишет отказ. Не переделывайте на таймер.

**Не заходить:** `RoleSeeder`, `EnsurePermission`, `routes/api.php` вне своих строк `api/mobile/*`; журнал, `AttendanceAnalysisService`, движок расписания, сидеры — читайте и переиспользуйте, не правьте.

---

## Чат «Единая карточка человека»

**Сейчас: `AUTH-003`, вход через Telegram. Начинать можно — бота ждать не нужно.**

Запрет снят: владелец заводит бота по инструкции [TELEGRAM_BOT_SETUP.md](../TELEGRAM_BOT_SETUP.md), она ему отдана. Живая проверка виджета по-прежнему возможна **только на `portal.skki.ru` после установки релиза** — у DEV самоподписанный сертификат, Telegram требует публично доверенного HTTPS. Но сам провайдер пишется и проверяется тестами без бота: подпись — HMAC-SHA256, поддельного токена для тестов достаточно, и это честнее, чем выдумывать за Telegram настоящий.

Четыре условия, которые уже зафиксированы и переизобретать их не надо:

- **Имена ключей `.env` названы владельцу заранее** — `TELEGRAM_LOGIN_BOT_USERNAME` и `TELEGRAM_LOGIN_BOT_TOKEN`. Назовите ровно так, иначе инструкция разойдётся с кодом.
- **Пустое значение в `.env` — пустая строка, а не «не задано».** `env('X') ?: 'по умолчанию'`, иначе настройка потеряется молча; на этом уже терялась настройка ФИС.
- **Правило слоя `AUTH-005` не меняется:** привязка только к существующей учётной записи, новая не создаётся никогда; отказ в привязке занятого аккаунта не сообщает, чей он.
- Токен придёт в `.local/secrets/telegram-bot.private.txt`. **В git, во frontend и в логи он не попадает** ни при каких обстоятельствах.

**Дальше:**

1. **`AUTH-002`, восстановление пароля — упирается в SMTP.** Нужен пароль ящика `portal@skki.ru` от владельца и решение по тем, у кого почты нет вовсе. Пока их нет, задача не начинается.
2. **`AUTH-004`, MAX — только после выбора владельца** между подтверждением через бота и порталом как mini-app. В записанном виде задача нереализуема, разбор в [AUTH_005_PLAN.md](../AUTH_005_PLAN.md).

Очередь прошлых заходов закрыта: Firefox (`df9563c`, дефекта у нас нет), проверка ИНН и СНИЛС (`040feba`), политика паролей — **владелец решил 11.08.2026** и решение уже реализовано (`d3ac7ad`): пятизначные выдаются по-прежнему, но после входа портал предлагает завести свой пароль, не короче шести символов, латиница с заглавной. Предложение, а не требование; напоминание возвращается при следующем входе, пока своего пароля нет.

**Не заходить:** `routes/api.php` вне своих строк, `RoleSeeder`, `EnsurePermission` — за областью ФИС. Мобильные экраны — за «Мобильными кабинетами».

---

## Чат «Учётные записи, роли и демо-данные»

**Сейчас: последние два пустых раздела из пяти — движок расписания и выпускники.**

Три закрыты и развёрнуты на стенде (`eb6f0ae`): журнал, учебные планы, нагрузка. Осталось ровно то, что вы сами и назвали причиной: **покрытие часов и конфликты читают `schedule_entries`, а набор пишет прямо в `schedule_lessons`**, поэтому покрытие по-прежнему пустое; выпускников набор не создаёт вовсе.

**Обход прав закрыт, и релиз им больше не держится** — спасибо. Заодно вы нашли то, чего никто не искал: прежний прогон на 610 запросов отвечал не на тот вопрос, роль `curator` вообще не имела учётной записи и не проверялась ни одним запросом. Две строки про кабинеты куратора и администратора допишет область мобильных кабинетов — кабинеты их, а вас прерывать ради двух строк дороже. **До её вливания таблицу разделов в скрипте не трогайте.**

**Дальше:**

1. **Замечание по ходу, разобраться и решить:** `PersonService` снимает у телефона плюс — `+79990000010` уходит в выгрузку и возвращается как `79990000010`. Номер не теряется, это правило единой точки записи. Но при построчной сверке файлов владельцем расхождение видно. Реши: приводить к единому виду на выгрузке, или объяснить в документации и оставить.
2. **Две карточки преподавателя у `teacher@local` на стенде** — `teachers.id = 2` пустая и `teachers.id = 5` от демонстрационного набора. `User::teacher()` — `hasOne`, берётся первая, поэтому мобильный кабинет и журнал у этой учётной записи выглядят пустыми. Набор привязал демо-преподавателя к существующей учётной записи; какую карточку оставить — вопрос владельцу, но подготовить его ваш.

**Не заходить:** `routes/api.php`, `EnsurePermission`, `RoleSeeder` — за областью ФИС. Контур авторизации — за «Единой карточкой человека». `api/mobile/*` — за «Мобильными кабинетами».

---

## Чат «ФИС ГИА и приёмная комиссия»

**Сейчас: два разбора для владельца — оба про право решать, а не про код.**

Очередь области пройдена: корзина закрыта целиком (`31b426c`), `HR-002` закрыт (`5bff006`) — кадры получают только тех людей, кого нашёл их собственный ввод, и только то, что их различает. Пункт про `GET api/mobile/student` снят: право у маршрута объявлено ещё в `ARCH-001` (`9bc9178`).

1. **`fis.outbound.create` у директора.** У него есть `fis.outbound.view` и `fis.outbound.status`, но нет `create`, поэтому проверка связи со шлюзом и обновление статуса ему закрыты. Право досталось этим маршрутам от таблицы префиксов, где POST выводился в «создание», и при переносе сохранено дословно — намеренно, чтобы ничего не расширить. **Подготовь владельцу решение:** что именно откроется, если убрать, и кому это нужно по работе.
2. **Конкурсы ФИС.** У программы их может быть несколько — бюджет и платное, очное и заочное, — а сопоставление хранит один UID на программу. Неоднозначные случаи сейчас не связываются и возвращаются списком. Разводить правильно — по форме обучения и источнику финансирования из `ProgramChoice`, но тогда конкурсы придётся вести в портале как сущность. **Это тоже разбор для владельца, а не код.**

Помни собственную находку: **новое право обязано появиться миграцией, а не только в `RoleSeeder`.** Сидер выполняется при установке и больше никогда, `installer/update.sh` запускает только миграции. На этом уже потерялись два права.

**Не заходить:** контур авторизации — за «Единой карточкой человека». Сидеры, отчёты, движок расписания — за областью демо-данных. Мобильные экраны — за «Мобильными кабинетами».

---

## Координирующий чат

Ведёт этот файл, собирает релизы, обновляет PROD, фиксирует решения владельца. Кода не пишет.

**Состояние на 11.08.2026, ствол `edb4c52`:**

- **`0.8.0-rc8` не собран.** Держит одно: два раздела в обходе прав и перезапуск на текущем стволе — за областью мобильных кабинетов. Придёт «ноль расхождений» — собирать.
- **Обход прав пройден на стволе `ffdb8fa`:** 693 запроса, ноль расхождений (`b7bda6a`). После него влились корзина — со своим обходом и тем же результатом, — два мобильных кабинета и наполнение демо-набора. Кабинеты обходом не покрыты, отсюда и остаток.
- PROD на `0.8.0-rc7`, `installer/VERSION` тоже `rc7`. Отставание больше сотни коммитов. В релиз войдут `ARCH-001` (переписанная модель доступа) и `SEC-002` (**разлогинит всех**) — час выбирает владелец, выкатка отдельным поручением.
- **Незакоммиченного нет нигде**, все области свободны от веток. На стенде DEV развёрнут наполненный демо-набор: журнал, учебные планы и нагрузка. Резервная копия до наполнения — `/tmp/college_portal_before_journal_20260811.sql` на DEV.
- **Решение владельца от 11.08.2026, записано:** выдаваемые пароли остаются пятизначными, но после входа портал предлагает завести свой — не короче шести символов, латиница с заглавной. Реализовано (`d3ac7ad`), кто ещё держит выданный, видно по колонке `must_change_password`.
- **Ствол движется по несколько коммитов в час** — за один заход координатора он ушёл на четырнадцать. Сверяйте этот файл со стволом перед тем, как на него опираться, и не считайте «Сейчас» действительным, если ваша область закрыла задачу час назад.
- Инструкция по боту Telegram владельцу отдана: [TELEGRAM_BOT_SETUP.md](../TELEGRAM_BOT_SETUP.md). Запрет начинать `AUTH-003` снят — без бота нельзя только живую проверку виджета.

**Порядок сборки релиза** — в `CLAUDE.md`, раздел «Ветки и релизы». Коротко: поднять `installer/VERSION`, обновить `CHANGELOG.md` и версии в документах, утверждающих текущее состояние; собрать на DEV при чистом дереве; тег **на коммит сборки**; выгрузить на PROD через `cp-agent`. Резервная копия снимается установщиком сама, путь из строки `Backup before update:` записать.

**Чего не делать:** не собирать релиз, пока в стволе идёт незавершённая работа по авторизации или правам; не разворачивать на PROD без отдельного поручения владельца.

## Что держит владельца

- **`AUTH-003`:** бот Telegram. **Инструкция готова** — [TELEGRAM_BOT_SETUP.md](../TELEGRAM_BOT_SETUP.md), пять минут в мессенджере. Токен в `.local/secrets/`, в чат не присылать.
- **ФИС:** учётные данные TEST — **только на шлюзовой узел**.
- **SMTP:** пароль ящика `portal@skki.ru`. Держит `AUTH-002` — восстановление пароля; вместе с ним нужно решение по тем, у кого почты нет.
- ~~Политика паролей~~ — **решено 11.08.2026 и реализовано.**
- **Firefox:** два шага в [SEC_002_BROWSER_CHECK.md](../SEC_002_BROWSER_CHECK.md). Пока их нет, замечание закрыто выводом «дефекта у нас нет».
- **Маршрут `/legacy`:** убрать или оставить аварийным входом — [LEGACY_INTERFACE_REVIEW.md](../LEGACY_INTERFACE_REVIEW.md). Заодно `HelloWorld.vue` с тремя картинками: мусор из шаблона Vite, удаляется без последствий.
- **Путь для MAX:** подтверждение через бота или портал как mini-app — [AUTH_005_PLAN.md](../AUTH_005_PLAN.md).
- **Приоритет по демо-набору:** журнал, учебные планы и нагрузка наполнены и стоят на стенде; осталось покрытие часов (движок расписания) и выпускники.
- **`fis.outbound.create` у директора** и **должна ли `study_records` видеть программы и специальности** — придут разбором от области ФИС.
- **Две карточки преподавателя у `teacher@local`** на стенде — какую оставить.

## Про релиз

**`rc8` собирается сразу после двух строк в обходе прав и перезапуска.** PROD на `0.8.0-rc7`, отставание больше сотни коммитов, среди них переписанная модель доступа (`ARCH-001`) и смена авторизации (`SEC-002`, разлогинит всех).

Развезти `ARCH-001` отдельным релизом уже нельзя — он в стволе. Обход прав на текущем стволе — последняя оставшаяся страховка.
