# Назначения по чатам

**Обновлено: 10.08.2026, сверено со стволом `2f1cd5c`.**

Этот файл ведёт координирующий чат по указанию владельца. Рабочие чаты его **читают, но не правят**. Соседний раздел «Кто чем занят прямо сейчас» в [ACTIVE_WORK.md](../ACTIVE_WORK.md) — наоборот: там пишут рабочие чаты о себе.

## Как пользоваться

1. **Сначала обнови ствол:** `git fetch dev && git merge --ff-only dev/develop`
2. **Найди свой раздел** по названию с доски, **не по номеру**: нумерация у владельца и на доске разная.
3. Делай «Сейчас», закончив — бери следующее из «Дальше», не спрашивая.
4. **Заявляй задачу на доске до того, как заводишь ветку.**
5. Назначение разошлось с реальностью — не выдумывай замену и не бери чужое, напиши владельцу.

## Как владелец называет чаты

| Владелец говорит | Раздел здесь |
| --- | --- |
| Первый | «Учётные записи, роли и демо-данные» |
| Второй | «Единая карточка человека» |
| Третий | «ФИС ГИА и приёмная комиссия» |

---

## Чат «Учётные записи, роли и демо-данные»

**Область:** контроллеры отчётов, сидеры, экраны, обработчики импорта, движок расписания.

**Сейчас:** аудит, **находка 9** — импортированное расписание не идёт в покрытие часов нагрузки. Перевести импорт на `ScheduleEngineService::apply`, чтобы создавалась `ScheduleEntry`, а legacy-запись появлялась зеркалом.

**Тем же заходом — экспорт расписания.** Его нет вовсе; после перевода на движок он почти бесплатный. Колонки брать у `ScheduleImportHandler`, критерий — файл выгрузки принимается импортом без правок. Промпт: [2_AUDIT_REMAINING.md](2_AUDIT_REMAINING.md).

Это **последняя открытая находка аудита**. Закроешь — из тринадцати останется только остаток 12, и он за другим чатом.

**Дальше:** [3_EXPORT_IMPORT_SYMMETRY.md](3_EXPORT_IMPORT_SYMMETRY.md) — шаблон преподавателя (сейчас девять колонок, ни даты рождения, ни СНИЛС, ни адреса) и обход остальных реестров на симметрию.

**Не заходить:** `RoleSeeder`, `EnsurePermission`, страницы студентов — за чатом ФИС. Контур авторизации — за чатом «Единая карточка человека».

---

## Чат «Единая карточка человека»

**Область:** контур авторизации.

**Сейчас: доказать, что `SEC-002` действительно работает.** Задача влита (`cc343c544`), но в браузере не проверена ни разу, а выкатка на PROD разлогинит всех. Отправлять непроверенную смену авторизации на боевой контур — верный способ закрыть себе вход.

Проверь на DEV в браузере и оформи результат так, чтобы владельцу осталось только повторить по шагам:

1. Вход, обновление страницы, выход — сессия живёт и умирает как положено.
2. Токена нет в `localStorage` и `sessionStorage`, из JavaScript он недостижим.
3. Запрос без признака CSRF отклоняется, с чужим — тоже.
4. Истёкший токен не пускает.
5. **Мобильный контур:** сканер `access/mobile-scanner` и личный QR-пропуск. Требуют secure context; камеру на телефоне проверяет владелец — дай ему короткий список шагов и адрес.

Нашёл расхождение — чини сразу, это твоя задача.

**Дальше:**

1. **Маршрут `/legacy` и остаток находки 12.** `frontend/src/App.vue` (3588 строк) открывается по `/legacy`, помеченному `public: true`, ссылок нет нигде. **Разбери, а не удаляй.** Отдельно проверь то, что стало острее после `SEC-002`: старый интерфейс может брать токен по-старому, из `localStorage`, — тогда он либо сломан, либо обходит новую защиту. Подготовь решение владельцу.
2. **`AUTH-005` — вход через Telegram и MAX.** `SEC-002` его разблокировал. Только привязка к существующей учётной записи, **никогда не создание новой**. Требует решений и настроек владельца — начни с плана и списка того, что от него нужно.

**Не заходить:** `RoleSeeder`, `EnsurePermission`, страницы студентов — за чатом ФИС. Движок расписания, сидеры, отчёты — за чатом демо-данных.

---

## Чат «ФИС ГИА и приёмная комиссия»

**Область:** права доступа и внешний обмен. Владеет `EnsurePermission::DOMAIN_RULES` и `RoleSeeder`.

**Сейчас:** [5_ARCH_001_PERMISSIONS.md](5_ARCH_001_PERMISSIONS.md) — `ARCH-001`, одна правда о правах вместо трёх списков.

**Запрет снят:** `SEC-002` влит (`cc343c544`), контур авторизации свободен. Планирование можно доводить до кода.

Требует показать план владельцу и дождаться подтверждения. Помни собственное наблюдение: **таблица прав только сужает доступ** — уточнение однажды сломало массовый экспорт у директора.

**Дальше:** `HR-002` — у кадров нет выхода, когда на человека находится несколько совпадений: `HrService::resolvePerson` просит выбрать вручную, а реестр людей кадрам закрыт намеренно, там же студенты и абитуриенты. Задача в твоей области. **Начни с вариантов для владельца,** а не с кода: нужен либо отдельный поиск в кадровой области видимости, либо разрешение конфликта прямо в карточке сотрудника.

**Не заходить:** контур авторизации — за чатом «Единая карточка человека». Движок расписания, отчёты, сидеры — за чатом демо-данных.

---

## Свободно, никем не занято

- **`PERSON-001`** — копии ФИО в `graduates` и `applicant_applications` в рассылку из Person не входят. Обе записи историчны; переписывать их — решение владельца, а не исправление.

## Запреты на одновременный запуск

Контур авторизации свободен: `SEC-002` влит, `ARCH-001` можно вести. Но **`AUTH-005` не начинать, пока идёт `ARCH-001`** — оба меняют вход и права, и столкнутся.

## Что делает координирующий чат

Сборка релизов и обновление PROD. Рабочие чаты релиз не собирают и PROD не трогают.

**PROD на `0.8.0-rc7`, отстаёт на 67 коммитов и 4 миграции.** Следующий релиз включит `SEC-002` и **разлогинит всех** — собирать только после того, как проверка в браузере пройдена.
