# Задача: разобрать оставшиеся находки аудита

Скопируйте текст ниже целиком в новый чат.

---

Прочитай `CLAUDE.md`, `docs/ACTIVE_WORK.md` (раздел «Осталось по аудиту») и `docs/AUDIT_2026-08-08.md`. Работай по правилам из `CLAUDE.md`.

## Что нужно сделать

Закрыть оставшиеся находки аудита от 08.08.2026.

**Уже закрыто, не переделывай:** находки **1, 2, 3, 4, 6, 8, 10, 11** и частично **12**.

Осталось четыре. Ни одна не блокирует работу и не портит данные — порядок ниже взят из отчёта и уточнён предыдущей сессией.

| Очередь | Находка | Что делать |
| --- | --- | --- |
| 1 | **9** | Импортированное расписание не считается в покрытии часов нагрузки. Импорт расписания перевести на `ScheduleEngineService::apply`, чтобы создавалась `ScheduleEntry`, а legacy-запись появлялась зеркалом. |
| 2 | **5** | Сводка и выгрузка отчёта проходной обрезаны 1000 событиями молча. Счётчики — агрегатом в базе; «только опоздавшие» и поиск — в SQL до ограничения; выгрузку стримить курсором; ограничение оставить только на списке экрана и подписать. |
| 3 | **7** | «Отчеты» — заглушка `PageStub`, экспорты журнала и кадрового календаря недостижимы. Либо перенести раздел, либо убрать пункт меню; кнопки экспорта журнала добавить — endpoints и права готовы. |
| 4 | **13** | Список эвакуации делает запрос к базе на каждого человека. **Сначала замерить под нагрузкой:** вывод сделан из чтения кода, не из измерения. Если не подтвердится — так и записать, а не «оптимизировать» вслепую. |

**Находка 9 первой не по важности, а по связности:** тот же перевод импорта на движок расписания открывает дорогу экспорту расписания, которого сейчас нет вовсе. Сделаешь одно — второе становится почти бесплатным. Экспорт расписания можно сделать здесь же; если решишь не делать, скажи об этом явно, чтобы задача по симметрии экспорта его подобрала.

## Что уже занято другим чатом

**Находка 5 (обрезка отчёта проходной 1000 событиями) — за чатом «Учётные записи, роли и демо-данные».** Он правит `AccessReportController`. Не берите её и не заходите в этот файл; проверьте по разделу «Кто чем занят прямо сейчас» в `docs/ACTIVE_WORK.md`, не освободилась ли она.

Ваши — находки **9, 7 и 13**.

## Что в эту задачу НЕ входит

- Полнота шаблонов преподавателей и обход остальных реестров — в задаче `3_EXPORT_IMPORT_SYMMETRY.md`.
- Перевод прав на объявление у маршрута — в задаче `5_ARCH_001_PERMISSIONS.md`.

## Незакрытое решение владельца

От находки 2 остался вопрос, который **не код, а решение**: должна ли роль `study_records` видеть образовательные программы и специальности. Сейчас получает `403`, хотя ведёт группы и выпуск. Права роли предыдущие сессии намеренно не расширяли.

Упрёшься — **спроси владельца, а не решай сам**: расширение прав роли меняет контур доступа.

## Грабли, на которые уже наступали

- **Таблица прав в `EnsurePermission` только сужает доступ.** Уточнение таблицы однажды сломало массовый экспорт у директора: `admissions/bulk/*` стал требовать `admissions.edit`. Массовые операции проверяют право на каждое действие внутри контроллера, поэтому на входе им поставлено право просмотра. Добавляешь префикс — проверь, кому этим закрыл дверь.
- **Вспомогательный запрос не имеет права закрывать экран.** Список для фильтра или подсказки либо запрашивается при наличии права, либо переживает отказ, а бессмысленный без него элемент не показывается вовсе.
- **Новый маршрут вноси в таблицу прав:** `PermissionTableAndProxyTest` обходит группу `manage_dictionaries` и падает на невнесённом префиксе.
- Чтение и запись CSV уже сведены в одно место — не заводи третье.
- Не заводи второй расчёт: полнота карточки — `AdmissionDocumentReadinessService`, присутствие в здании — `AccessPresenceService`.

## Проверки

Тесты на DEV в отдельном worktree, ветка от `origin/develop`, прогон на SQLite и на PostgreSQL 17. Каждую закрытую находку покрывай тестом, воспроизводящим её.

Находку 7 после исправления посмотри **в браузере на DEV**: она про экраны, а не про API. Учти, что **стенд почти пуст** — 2 студента, 3 преподавателя, 0 занятий и 0 событий проходной, — поэтому для находок 5 и 13 данные под нагрузкой придётся создавать самому.

## Когда закончишь

Коммить и пушить с DEV, отметить закрытые находки в `docs/AUDIT_2026-08-08.md`, обновить `docs/ACTIVE_WORK.md`, затем **обновить локальную рабочую копию**:

```bash
git fetch dev && git merge --ff-only dev/develop
```

На PROD ничего не разворачивать без отдельного поручения.
